# Hermes ↔ Codex Handshake – 2026-09-06

## Direct runtime result

Hermes was started in a fresh one-shot session from `C:\Hermes\workspaces\owner-control` with the redacted profile and source manifest as its explicit reading task. Hermes read both files and returned the following verified summary in German:

- Björn is Owner and CEO of the automation and software environment.
- Hermes is the local orchestration center.
- Codex is the technical execution and review colleague through Codex-MCP.
- The continuing source set includes `C:\Hermes`, the Hermes memory/configuration, operations handovers, ADAM, Platform-Reuse, Codex metadata, screenshot evidence, and the Hostinger shared knowledge database, VPS Hermes runtime, OpenClaw, Ollama and gateway endpoints.
- The requested “Open Core” topic is carried in the handover as the OpenClaw/Open-Core gateway layer; Hermes must verify the concrete product/process identity live instead of conflating similarly named components.
- Secrets, cookies, private keys, bot tokens, API-key values and `.env` values must never be stored.

## Persistence result

Hermes then used its memory tool in a second fresh session and returned:

> Persistiert (dedupliziert, kein Duplikat vorhanden). Status: gespeichert.

The resulting fact is present in `C:\Hermes\memories\MEMORY.md` under `Codex-Hermes-Handshake 2026-09-06`.

## MCP execution verification

A second fresh Hermes session enabled `codex-executor` together with file and memory tools. Hermes called the Codex-Executor MCP and received a read-only runtime status report. Hermes explicitly confirmed that Codex is its technical execution and review colleague and that no secrets, cookies or private keys were transferred. The MCP handshake is therefore verified at both connection level and agent-use level.

The Platform-Reuse-Catalog MCP required a Windows-specific attestation repair: Hermes starts the stdio adapter through an intermediate agent process, so the adapter now validates the bounded Windows process ancestry against the gateway-owned `C:\Hermes\gateway.pid` record. The live gateway canary recorded `runtime_attestation_status=PASS`, `runtime_consumer=Hermes Gateway`, and `external_writes=false`. A complete catalog readback remains a separate acceptance item until all required read-only catalog tools have been invoked by the gateway runtime.

## Ongoing operation

- Hourly: `Hermes inkrementeller Wissens-Refresh` (`17727160b910`) runs the redacted metadata monitor without an LLM, persists its state, and emits only changed paths/counts.
- Daily: `Hermes tägliche Voll-Reconciliation` (`c955bd126543`) is scheduled for a broader German-language reconciliation of profile, handovers, local sources, VPS, MCP, OpenClaw, n8n, Telegram, skills, memory, provider routing and GitHub.
- Both jobs deliver locally, do not send external messages, and do not authorize customer/public/payment actions.

## Current limitation

The OpenRouter Free quota is exhausted for the current account. The hourly job therefore remains deterministic and cost-free. The paid DeepSeek alias is reserved for bounded, intentional reconciliation/handshake calls under the Hermes-G weekly key limit.
